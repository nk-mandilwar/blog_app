<% if !current_user || !Subscribe.exists?(:email => current_user.email) %>
  <div class="subscribers-div">
    <div class="subscribe">
      <p> <strong>To get all news, Subscribe it.</strong> </p>
      <p> <button class="btn btn-primary subscriber-button">Subscribe</button></p>
    </div>
    <div class="subscriber-form">
	    <%= render "layouts/subscribe_form" %>
    </div>
  </div>
<% end %>
